image: node:23.11.0

pages:
  stage: deploy
  cache:
    paths:
      - node_modules/
  script:
    - npm install
    - npm run build
    - mv docs/.vitepress/dist public
  artifacts:
    paths:
      - public
  only:
    - master
